
[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q -v"
testpaths = [
    "tests",
    # "integration",
]
DJANGO_SETTINGS_MODULE = "delivery_router.settings"
python_files = "tests.py test_*.py *_tests.py"
markers = 'unit'

